# ESPN Fantasy
ESPN_S2=your_espn_s2_cookie_here
SWID=your_swid_cookie_here
LEAGUE_ID=your_league_id_here

# Snowflake
SNOWFLAKE_ACCOUNT=your_account_here
SNOWFLAKE_USER=your_user_here
SNOWFLAKE_DATABASE=your_database_here
SNOWFLAKE_SCHEMA=RAW
SNOWFLAKE_WAREHOUSE=your_warehouse_here
# Output scripts read from the dbt-built schema. Defaults to ANALYTICS.
# Override only if your dbt target schema is named differently.
# SNOWFLAKE_ANALYTICS_SCHEMA=ANALYTICS

# Auth -- pick ONE of the two paths:
#
# Path A: key-pair authentication (recommended; required if the account
# has MFA enforced). Set the path to your private key file. Optional
# passphrase for encrypted keys. See SETUP.md section 4 for key
# generation + public-key registration steps.
SNOWFLAKE_PRIVATE_KEY_PATH=/path/to/your/.snowflake_rsa_key.p8
# SNOWFLAKE_PRIVATE_KEY_PASSPHRASE=only_set_for_encrypted_keys
#
# Path B: password authentication. Only works if MFA is NOT enforced
# on the account. Leave SNOWFLAKE_PRIVATE_KEY_PATH unset/empty to use
# this path; the connector falls back to password when no key is set.
# SNOWFLAKE_PASSWORD=your_password_here

# Google Sheets -- optional. Configure both to enable Sheets output
# from generate_records_report.py. Leave blank/unset to skip; the
# script no-ops on the Sheets sink when SHEETS_OUTPUT_ID is empty.
# See SETUP.md for the GCP project + OAuth client creation walkthrough.
GOOGLE_OAUTH_CLIENT_PATH=
SHEETS_OUTPUT_ID=
