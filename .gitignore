# Secrets
.env
*.env
!.env.example
ESPN Fantasy Baseball Cookies.txt
# Phase 6.3: cached OAuth token from sheets_writer's first-run user-flow.
# Stored next to the script as .sheets_oauth_token.json; gitignored to
# keep per-user credentials out of git.
output/.sheets_oauth_token.json

# Python
.venv/
venv/
__pycache__/
*.pyc
*.pyo
*.egg-info/
.pytest_cache/

#Test Scripts (legacy research scripts at repo root only; the tests/
# directory is intentionally tracked — pytest discovers it via pytest.ini)
extract/debug_stats_keys.py
/test_*

#Customized League Note
output/LeagueNote.txt

# dbt
target/
dbt_packages/
logs/
.user.yml

# IDE
.vscode/
.idea/
*.swp

# OS
.DS_Store
Thumbs.db
.claude/

# Data
*.csv
*.json
!dbt_league/**/*.json
!config/*.json
!archive/*.json
!dbt_league/seeds/*.csv