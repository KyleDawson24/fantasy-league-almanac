# Docs & Portfolio

**Linear:** [Docs & Portfolio](https://linear.app/roguelitedevelopment/project/docs-and-portfolio-19a3c0004b34) · Backlog · Medium (continuous + capstone)

The continuous thread — every session leaves docs accurate — plus the
capstone: a dedicated readable/intuitive/accurate documentation session
after CBS & Yahoo land (dbt docs polish, possibly an Obsidian-style
published layer, full read-through in reviewer posture).

Also home to two standing decisions:

- **MetricFlow:** current recommendation is skip — absence is not a smell an
  interviewer detects (grain tests + exposures + doc blocks already read
  senior); a half-adopted semantic layer is. Revisit when the Public
  Dashboard needs metrics defined once across consumers; adopt narrowly if
  adopted.
- **Roadmap sync:** today, board state mirrors to these briefs + ROADMAP.md
  by convention in the same commit as any session that changes it; a small
  Linear-API sync script automates that later and is itself a portfolio
  artifact.

**Depends on:** the capstone waits for Multi-Platform's CBS + Yahoo
milestones; everything else is continuous.

**Seeded issues:** MLB-27 MetricFlow decision · MLB-28 docs deep-clean
capstone · MLB-29 roadmap sync automation
