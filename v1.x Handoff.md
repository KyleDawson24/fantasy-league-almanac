# v1.x Working Brief — post-v1.0 development

Written at v1.0.0 ship. Forward-looking for the next development cycle.

For the historical retrospective on what got the project to v1.0, see
`Phase 7 Documentation.md`. For the public-facing forward-looking
roadmap, see `ROADMAP.md`. This doc covers what those don't: the
internal "where to start," product decisions still on the table, and
conventions worth preserving.

---

## Status at v1.0 ship

- Tag `v1.0.0` published; GitHub Release live with notes
- 47 Phase 7 commits on `origin/main` (pre-Phase-7 was at `a1470e0`;
  v1.0 ship at `1c7a9f9`)
- dbt docs catalog hosted at
  https://kyledawson24.github.io/fantasy-league-front-page/
- All Phase 7 review-pass findings (P1-P3) resolved or explicitly
  deferred to ROADMAP

What's verified working at ship:

- `dbt build`: 45 PASS / 0 ERROR
- `pytest tests/`: 112 passed, 15 deselected
- `pytest tests/ -m warehouse`: 15 passed (incl. byte-diff golden BBCode
  for both summary + records report)
- Weekly recap generates byte-identical to the pre-rearchitect baseline
  (Week 6 2026)

---

## Status at v1.0.1 ship (2026-05-18)

Polish release on top of v1.0.0. Shipped from the "v1.x priorities"
list below:

- **Stat Catalog Cleanup** ✓ shipped (`is_always_tracked` →
  `auto_tracked`, NEGATIVE_POINTS as record candidate, stat 30 / Hit
  for the Cycle promoted + `cycles` callout).
- **Recap Polish** ✓ all three sub-items shipped (round-at-fact-layer
  killed the float wobble, conditional Top Scorer line, "None recorded
  yet" rendering for floor-zero stats).
- **Records Logic Cleanup** ✓ DI shipped (count_value_occurrences
  injected via `count_fn` parameter; `records_logic` is now
  import-pure with respect to the data layer; saturated-tier branch
  unit-testable). The `output/_setup.py` boilerplate factoring was
  intentionally skipped — Phase 7's `db.py::init()` already absorbed
  the substantive part; what was left wasn't worth the indirection.
- **League-Wide Benchmarks** ✓ shipped — `mart_league_weekly_
  benchmarks` mart + always-on `League This Week:` recap line +
  `hot_week` / `cold_week` callouts.

Adjacent flavor / operational work also shipped in 1.0.1:

- 8 new `league_notes.py` callouts (`cycles`, `no_quality_starts`,
  `hr_streak_active`, `hr_streak_ended`, `hero`, `scapegoat`,
  `mismatch`, `no_negative_days`).
- Matchup-outcome callouts (Tough Luck / Lucky Bastard / Fair-and-
  Just) migrated from inline `generate_summary.py` into the
  `CALLOUTS` registry. Single home for all conditional flavor.
- Snowflake key-pair authentication support (`output/db.py` and
  `extract/extract.py`) — required after MFA enforcement on the
  account. See SETUP.md §4.

Still open from the v1.x priorities list (next):

- **Player Profiles** (flagship) — `dim_player` + `fct_player_career`,
  the player-as-entity layer the project doesn't have yet.
- **Sheets Sink Hardening** — parked, then dropped. Upcoming Sheets
  surface redesign supersedes.
- **Owner Names in the Mart** — wire `owner_nicknames.csv` through
  stg/int.
- **Playoff Contention Identification** — needs ESPN-API discovery.
- **Calendar Auto-Populate** — derive `matchup_schedule` from ESPN
  settings; small `_overrides` seed for `is_abnormal`.
- **Career Stats Per Team** — `fct_team_career_stats` mart;
  companion to the player-profile flagship.

---

## Status at v1.0.2 ship (2026-05-19)

Refactor-only release on top of v1.0.1. No consumer-visible behavior
change — byte-diff golden BBCode regression unchanged pre/post. What
changed is the dbt DAG itself: new contract layer between seeds/int
and consumers, a promotion from intermediate to mart, and a bunch of
denormalization that simplifies downstream filtering.

What shipped:

- **New mart-layer contracts.** `dim_stat` (over `stat_classification`),
  `dim_matchup_period` (over `matchup_schedule`), and
  `fct_player_daily_performance` (over `int_player_daily`). Output
  scripts and dbt models now route through these instead of reaching
  past the marts layer.
- **`int_player_weekly_performance` promoted** to
  `fct_weekly_player_performance` — renamed, moved from
  `intermediate/` to `marts/`, re-sourced from
  `fct_player_daily_performance`. Materialized as a table (was a
  view). The daily-to-weekly DAG edge is now load-bearing.
- **Schedule columns denormalized onto the four weekly facts.**
  `is_abnormal` / `is_playoff` / `playoff_round` now live as columns
  on each weekly fact (sourced from `dim_matchup_period`). Consumers
  filter abnormal weeks and render week labels directly off rows;
  several historical-count SQL queries dropped their
  `matchup_schedule` JOINs as redundant.
- **Single source of truth for the seed → leaderboard name
  translation.** `dim_stat.leaderboard_name` is the only place the
  mapping lives now. `output/stat_catalog.py`'s `SEED_TO_LEADERBOARD`
  + `to_leaderboard_name` removed; the duplicate CASE block in
  `mart_stat_leaderboard.sql`'s compile-time loop removed (it now
  reads `leaderboard_name` from dim_stat via `run_query`).
- **DAG hygiene.** dbt exposures rewired through the new contract
  layer (no more direct edges from seeds/int to consumer outputs);
  `weekly_recap` gains `mart_league_weekly_benchmarks` (which was
  added at v1.0.1 but never declared on the exposure);
  `mart_stat_leaderboard.current_year` CTE moved from `raw.box_scores`
  to `fct_weekly_team_active_performance` (one less raw edge on the
  visible DAG).

Verification at ship: dbt build 116 PASS / 0 ERROR; pytest 113 default
+ 15 warehouse including byte-diff golden BBCode regression.

Adjacent doc changes:

- `WISHLIST.md` renamed to `BRAINTHOUGHTS.md` and restructured to four
  sections (Wishlist / Clarifications / Discussions and Tweaks /
  Interview Questions). Still gitignored. Memory file
  (`project_brainthoughts.md`) added with the standing
  "review-at-every-push, never-delete-just-OBSOLETE" convention plus
  the narrow CLARIFICATIONS carve-out (replaced-architecture entries
  can be trimmed if no teaching moment survives).

Still open from the v1.x priorities list (next):

- **Player Profiles** (flagship) — `dim_player` + `fct_player_career`.
  This is the natural next chunk; the v1.0.2 cleanup laid the
  contract-layer groundwork that `dim_player` slots into cleanly.
- **Career Stats Per Team** — `fct_team_career_stats`; pair with
  flagship.
- **Owner Names in the Mart** — `owner_nicknames.csv` through stg/int.
- **Playoff Contention Identification** — ESPN-API discovery TBD.
- **Calendar Auto-Populate** — derive `matchup_schedule` from ESPN
  settings; `dim_matchup_period` already absorbs this re-source
  cleanly once it lands.

Smaller follow-on items the v1.0.2 work named but deferred:

- `format_week_label` could now read schedule columns directly off
  fact rows (the columns are denormalized as of v1.0.2). Currently
  still takes `schedule_lookup` for backward compat. Deletion of
  `records.load_schedule_lookup()` becomes possible once all callers
  migrate.
- Composite "wasted points" metric (recap's existing concept that
  combines inactive positives + active negatives) could move from
  Python math in `get_wasted_points` into a thin dbt view. Decision
  deferred to v1.1.0-era when it has natural pairing with other
  consumer-migration cleanup.
- `fct_weekly_team_active_performance` reads `raw.box_scores` directly
  (three references for the wrapper's home_score). By design per
  Phase 4 (platform_calculated_delta concept); future refactor could
  introduce an intermediate model that exposes home_score per
  team-MP. Documented in BRAINTHOUGHTS.md, not on the roadmap.

---

## v1.x priorities

Items pulled from `ROADMAP.md` Now bucket plus open product calls.
Each has a plain-English header for skim, then the technical detail
beneath.

### **Player Profiles** (v1.x flagship)
Build the player-as-entity layer the project has been missing.
`dim_player` is a slowly-changing dimension carrying current MLB team,
position, eligibility — absorbs the per-day display lookups that
consumer scripts currently reach into `int_player_daily` for.
`fct_player_career` is the career-aggregate fact (total fantasy teams
played for, career stats, transaction history derivable from box
scores). Most data is already in the pipeline; bio/draft additions are
a v2.x extract follow-on.

### **Stat Catalog Cleanup**
Three small changes to `stat_classification.csv` that have been
queued since Phase 7 surfaced them:
- Split `is_always_tracked` into `is_record_force_surface` +
  `is_tracked` (current column does double duty; the conflation
  surfaced during Phase 7 B1 review)
- Flag `NEGATIVE_POINTS` as a record candidate (column exists on all
  4 facts; just needs the seed flip to surface "Most Negative Points"
  callouts in the recap)
- Promote stat 30 (Hit for the Cycle) from `is_record_candidate=false`
  to true; pair with a `league_notes.py` "First cycle of the season!"
  callout. Discovered during Phase 7 F-prep (15-pt scoring weight,
  2 real candidates over 2 seasons)

### **Recap Polish**
Smallish output-rendering tweaks, no architectural risk:
- "No active no-hitters yet" rendering for stats where rank-1 = 0 with
  `is_always_tracked=false`. Currently the records section reads as if
  a record exists when none does
- Conditional 3rd "Top Scorer" line — drop it when the overall winner
  didn't have both non-zero hitting AND pitching contributions (it's
  often a duplicate of Top Hitter or Top Pitcher otherwise)
- Round one-decimal points at the fact layer to eliminate the cosmetic
  126.9 ↔ 127.0 ping-pong on summation-order changes

### **Sheets Sink Hardening**
The `_replace_tab` flow currently does `worksheet.clear()` + `update()`
which wipes user-applied formatting (colors, frozen rows, column
widths). Switch to in-place `update()` for the data range plus
targeted `batch_clear` for trailing rows when the new dataset is
smaller. Verify by hand-formatting a tab, running, and confirming
formatting survives.

### **Records Logic Cleanup**
Two code-organization tweaks:
- Dependency-inject `count_value_occurrences` into `collapse_ties`.
  Right now `records_logic.collapse_ties` has one explicit
  `from records_data import count_value_occurrences` for the
  saturated-tier backfill — the only logic→data import. Replacing it
  with a `count_fn` parameter keeps `records_logic` truly pure and
  enables unit-test coverage of the saturated-tier branch
- Factor shared boilerplate (`output/_setup.py`): UTF-8 stdout
  reconfig + dotenv + schedule lookup are duplicated across both
  consumer scripts

### **Owner Names in the Mart**
Wire `owner_nicknames.csv` into the dbt staging layer so owner display
names propagate through the mart. Today the seed exists but isn't
joined; output scripts read it ad-hoc (works, but the mart should
carry the canonical value).

### **Playoff Contention Identification**
During playoff weeks, all teams still play (consolation bracket); the
records output today can't distinguish a Finals MVP performance from
a 13th-place consolation week. Identifying which (team, season,
matchup_period) tuples represent actual playoff contention would let
records filter or annotate accordingly. Discovery TBD on what
playoff-bracket data ESPN's API exposes — may push to v2.0 if it
needs a new extract path.

### **Calendar Auto-Populate**
`matchup_schedule.csv` is currently a hand-maintained seed (~25 rows
per season). Most fields are derivable from
`espn-api`'s `league.settings`: `matchup_periods` dict, `regular_
season_count`, `playoff_team_count`. Only `is_abnormal` (All-Star
break, weather-shortened weeks) stays manual. Proposed shape:
extract → `raw.matchup_schedule` (append-only); new
`stg_matchup_schedule` joins to a tiny `matchup_schedule_overrides.csv`
seed for `is_abnormal` patches. Maintainer has additional automation
heuristics — confirm scope before writing code.

### **League-Wide Benchmarks**
Records today are per-team. Ungrouping for league-wide weekly
aggregates ("this week we averaged 250 points, 70th percentile in
league history") unlocks collective-mood callouts in the recap. Small
change — new view over existing facts.

### **Career Stats Per Team**
`fct_team_career_stats`: career-aggregate counterpart to
`fct_weekly_team_active_performance`. Powers "who has the most points
scored for their team in league history" callouts — fun for the
keeper-league framing. Data's already there; this is just an
aggregation layer. Companion to the `fct_player_career` flagship work.

---

## Directional v1.x — things to start or think toward

Items I want to make progress on during v1.x without committing to
specific deliverables. Some will mature into named work; others stay
exploratory until v2.x.

### **Run It On a Schedule**
Automate execution of the weekly pipeline. Currently extract → dbt
build → outputs is a manual Sunday ritual. Should be a cron job,
GitHub Actions workflow, or small scheduler that fires the whole
sequence and surfaces the BBCode for paste. Open question on where
the runner lives (local cron + a notification, GitHub Actions with
Snowflake secrets, a hosted scheduler) and how to deal with ESPN
cookie expiration mid-flight.

### **Yahoo / CBS Exploration**
Begin scoping a second extract source. Full delivery is v2.0 territory
per ROADMAP; exploration starts in v1.x. Read whichever Yahoo wrapper
is current, sketch what `extract/yahoo/` would look like, identify
where the staging-layer contract needs to flex to accept a second
normalization. Won't ship a working pipeline against a non-ESPN
league this cycle, but should know what it would take.

### **Recap Flavor Callouts (evergreen)**
The recap is never "done." Expect tweaks every cycle as observation
yields new color-callout opportunities. For this v1.x run specifically:
weave in the league-level benchmarks (above), expand the
`league_notes.py` registry with more callout types beyond the existing
no-hitter pattern (cycle promotion is the natural next), and start
linking from the recap to the Sheets dump when a deeper drilldown
warrants. Treat this as a standing item, not a discrete task.

### **Sheets v2 — Granular Data + History**
Expand the Sheets sink beyond the current 17-col / 3-tab layout. New
tabs for team history, per-stat drilldowns, career rollups (composes
with the `fct_player_career` + `fct_team_career_stats` work above).
Sheets Sink Hardening (above) keeps the existing tabs stable; this is
the additive expansion.

### **Pre-2.0 Portability Bridge**
Final v1.x piece before tagging v2.0. Either bring DuckDB target
online (per v2.0 ROADMAP) or ship setup-streamlining prompts/scripts
that lower the bar for someone else running this against their own
league — likely both, with one shipping first. The DuckDB direction
removes the cloud-warehouse prerequisite; the prompts direction makes
SETUP.md actionable rather than just readable.

### **Frontend in Mind, Not in Hand**
Operate v1.x architectural choices with potential interactive
frontend / dashboard in mind, but no public deliverable until v3.x
at earliest. Practical implication: when adding mart columns or
facts, ask "would this be useful to render somewhere interactive?"
If yes, don't over-couple it to the BBCode or Sheets surfaces.
Frontend stays an "aware of, not building toward" consideration
through v1.x and v2.x.

---

## Open product calls

These weren't resolved in Phase 7 and need a decision before the
relevant work can proceed.

### **PLATFORM_* records in the recap**
Phase 7 G2+G4 widened `SCORE_STAT_NAMES` to include `PLATFORM_*` —
which means the recap's records section may surface platform
records alongside calculated records. Defensible (shows both lenses),
but muddies the "calculated = normalized cross-season; platform = ESPN
authority for W/L" framing. To revisit: whether platform records
should surface in the recap, or stay confined to mart_stat_leaderboard
for ad-hoc analysis.

### **Active negatives as gross-vs-net waste**
Phase 7 closed: stay net-week for the `doubly_wasted_pts` recap
callout. The new per-day `negative_points` column on the active facts
remains as a data-layer artifact for future gross-negative analysis;
not actively consumed today. Investigation tool at
`tools/analyze_negative_metrics.py` regenerates the top-10 divergence
table if the call is revisited (mostly relievers — classic "one bad
outing offset by good ones" pattern).

### **Inactive fact column symmetry**
The `fct_weekly_*_inactive_performance` models surface only
`calculated_*` totals + grain dims, not the full counting / per-stat
`*_pts` columns their active counterparts expose. Two paths: full
mirror (extend inactive facts to match active schema) vs intentional
asymmetry (keep inactive terse, document the omissions formally).
Worth deciding before adding a consumer that needs inactive-grain
stat detail.

---

## Decided Against

Mirrored from `ROADMAP.md`. Listed here for completeness so a fresh
reader doesn't need to bounce out to ROADMAP to know what's been
considered and rejected.

### **Frequency-table tab**
Considered and rejected during Phase 6.3.3. The tie-collapse pattern
in the existing records output already handles the underlying need
(showing common stat values) without a separate consumer surface.

### **Player-grain rate stats at the mart layer**
Phase 6.3.3 Path A intentionally dropped player-grain rate stats from
`mart_stat_leaderboard` rather than gate them behind sample-size
thresholds. Team-grain rates stay because natural denominator
accumulation keeps them meaningful; player rates would need per-stat
threshold tuning to avoid small-sample noise — diminishing return.

---

## Conventions worth preserving

These worked in Phase 7. Keep them.

- **Commit messages: first-person, no AI attribution.** The pre-push
  sweep stripped Co-Authored-By trailers and reworded a few
  internal-y subject lines. New commits should match the style:
  technical, clear, no "the user" / "we" / Claude references.
- **Per-piece review cadence.** Big drafts (CHANGELOG, README, Phase
  retrospectives) get shown for review before commit. Small mechanical
  changes commit directly. Push to origin happens explicitly, not
  automatically.
- **dbt build + warehouse pytest before any model change ships.**
  Golden BBCode regression catches output drift; treat a red test as
  a stop-the-line.
- **Phase doc convention for major retrospectives.** Substantive
  rearchitects get a `Phase X.Y Documentation.md` entry. v1.x doesn't
  necessarily need this if the work is incremental polish — only if a
  v1.x release is genuinely Phase-shaped.
- **Current-state framing in `schema.yml` descriptions.** Don't re-add
  Phase X.Y prefix references; chronology lives in CHANGELOG and Phase
  docs. Same convention for `output/*.py` module docstrings.
- **"Decided Against" rather than "Won't Do"** in ROADMAP. The phrase
  signals "we considered this and the answer is no for these reasons,"
  which reads better than dismissive phrasing.

---

## Tooling notes

- **`gh` CLI installed at `C:\Program Files\GitHub CLI\gh.exe`.** Not
  on PATH by default; in PowerShell either prefix with `& ` or add to
  `$PROFILE`. Authenticated as of v1.0 ship.
- **`git filter-repo` not installed.** The Phase 7 commit-message
  sweep used `filter-branch` (works for ~50 commits; deprecated for
  larger ops). For any future history rewrite, `pip install
  git-filter-repo` first; cleaner API and faster.
- **dbt docs catalog regeneration:** `dbt docs generate --static` then
  `cp dbt_league/target/static_index.html docs/index.html`, commit and
  push. README documents this. Auto-regen via GitHub Actions is a v1.x
  candidate (would need Snowflake credentials in repo secrets).
- **Pre-Phase-7 worktrees pruned**; only `main` (the live one),
  `phase-3.2` (long-lived per HANDOFF §8), and any active session
  worktree should remain.
- **Memory files:** `~/.claude/projects/C--Users-kyled-projects-espn-league-manager/memory/`
  — `feedback_background_tasks.md` and
  `feedback_test_running_side_effects.md` are the most operationally
  load-bearing.

---

## Don't-touch list (operational)

These are committed product decisions; changing them breaks the
weekly post or league members' expectations. Surface before merging
any change that touches them.

- Section ordering in `generate_summary.py` (locked Phase 5)
- Header conventions: `[u][b]Section[/b][/u]`, `[b]Label[/b]: value`
- Player-card shape: `Player (TeamAbbr), X.X pts -- {stats}`
- Records show owner names; recap doesn't (audience separation)
- "Matchup #N" → "Week N" everywhere
- Playoff weeks render as round names (`Round 1` / `Semi-Finals` /
  `Finals`), not as `Week 24-26`
- 17-column Sheet schema across 3 tabs
- `SHEETS_OUTPUT_ID` env-var opt-in for the Sheets sink (not opt-out)
- `output/LeagueNote.txt` appended verbatim under "Additional Notes"
- BBCode renderer constraint: ESPN supports only `[b]` `[u]` `[i]`;
  no hyperlinks, images, or other tags

---

## Where to start a fresh session

If you're picking up cold and want to ship something:

1. **Read** `ROADMAP.md` for the full forward-looking map. Then this
   doc for the internal/decision context that ROADMAP doesn't carry.
2. **Pick** one v1.x item from the priorities list above. The Player
   Profiles flagship is highest-leverage; the Recap Polish items are
   smallest-effort if you want quick wins.
3. **Verify** environment: `dbt debug` from `dbt_league/`, then
   `pytest tests/` should pass cold.
4. **Branch** off main as `claude/<descriptor>` (or whatever
   convention works); do not commit to main directly.
5. **Don't push** to `origin/main` without explicit go-ahead. Same
   rule as Phase 7.

Welcome back.
